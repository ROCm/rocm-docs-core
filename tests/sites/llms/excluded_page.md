---
myst:
  html_meta:
    "description lang=en": "A page excluded from llms-full.txt but kept in the index."
---

# Excluded page

UNIQUE_EXCLUDED_BODY_MARKER: this prose must not appear in llms-full.txt.

```cpp
__global__ void excluded_kernel(int* a) { a[0] = 0; }
```
